language: scala

jdk: openjdk11

scala:
  - 2.13.1
  - 2.13.2
os:
  - linux
  - osx

before_install:
  - sudo apt update
  - sudo apt install openjfx
  - export DISPLAY=:99.0

install: true

script:
  - sbt ++$TRAVIS_SCALA_VERSION test